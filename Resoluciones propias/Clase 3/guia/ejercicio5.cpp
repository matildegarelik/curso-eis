#include <iostream>
#include <cmath>

using namespace std;

float a, b;
float ya=0;
float yb=0;
float Integral (float a, b, ya, yb)
int main(){

    char funcion[];
    cout<<"Ingrese una funcion f(x) para calcular su integral definida"<<endl;
    cin>>funcion;
    cout<<"Indique punto inicial de intervalo"<<endl;
    cin>>a;
    cout<<"Indique punto final de intervalo"<<endl;
    cin>>b;
    cout << "La integral definida en ese intervalo es "<< Integral(a,b,ya,yb)
}

float Integral (float a, b, ya, yb){
    ya = 
    yb =
    float resultado;
    resultado = (b-a)*0.5*(ya+yb)
    return resultado
}